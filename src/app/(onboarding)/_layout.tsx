import { Stack } from 'expo-router';

/** Onboarding stack (2FA + Stripe payout). Stubbed in Phase 0; native in Phase 7. */
export default function OnboardingLayout() {
  return <Stack screenOptions={{ headerShown: false }} />;
}
